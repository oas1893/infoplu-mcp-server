# Base URL for the Géoportail de l'Urbanisme API
# Default: https://www.geoportail-urbanisme.gouv.fr/api
API_BASE_URL=https://www.geoportail-urbanisme.gouv.fr/api

# Transport mode: "stdio" for local CLI use, "sse" for Docker/VPS HTTP deployment
# Default: stdio
TRANSPORT=stdio

# Port for SSE/HTTP transport (only used when TRANSPORT=sse)
# Default: 3000
PORT=3000
